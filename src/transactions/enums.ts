// src/transactions/enums.ts
export enum Industry {
    GENERAL_GOODS_SERVICES = 'GENERAL_GOODS_SERVICES',
    // Add other industry types as needed
  }
  
  export enum Currency {
    ZAR = 'ZAR',
    // Add other currencies as needed
  }
  
  export enum FeeAllocation {
    SELLER = 'SELLER',
    BUYER = 'BUYER',
    // Add other fee allocations as needed
  }
  